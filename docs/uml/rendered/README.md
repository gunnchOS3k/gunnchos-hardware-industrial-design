# Rendered UML

Optional PlantUML SVG output. GitHub-visible source of truth is the Mermaid Markdown under `docs/uml/current/`.

Run `./docs/uml/render_plantuml.sh` if `docs/uml/.tools/plantuml.jar` is present.
